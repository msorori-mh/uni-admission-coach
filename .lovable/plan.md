

## توسيع دائرة الشعار

### المشكلة
الدائرة المحيطة بالشعار حالياً بحجم `w-40 h-40` (160×160 بكسل)، مما يقص جملة "مُفَاضَلَة... هندسة النجاح في اختبارات القبول" الموجودة داخل صورة الشعار.

### الحل
توسيع حاوية الشعار الدائرية في الصفحات الثلاث:

#### 1. `src/pages/Index.tsx` (السطر 61)
- تغيير `w-40 h-40` إلى `w-56 h-56` (224×224 بكسل) لإظهار النص الكامل داخل الشعار

#### 2. `src/pages/Login.tsx`
- توسيع حاوية الشعار من `w-20 h-20` إلى `w-28 h-28`

#### 3. `src/pages/Register.tsx`
- نفس التوسيع كصفحة تسجيل الدخول

### النتيجة
ستظهر جملة "مُفَاضَلَة... هندسة النجاح في اختبارات القبول" بالكامل داخل الدائرة بدون قص.

