

## إصلاح تداخل نص الشعار مع حافة الدائرة

### المشكلة
سطر "مُفَاضَلَة... هندسة النجاح في اختبارات القبول." (سطر 68-70) يظهر قريباً جداً من الدائرة لأن العنوان `h1` بينهما يحمل فقط `mb-4`.

### الحل
- **سطر 65**: زيادة المسافة السفلية للعنوان `h1` من `mb-4` إلى `mb-6` لإبعاد الفقرة عن منطقة الشعار الدائري.

### الملف المتأثر
`src/pages/Index.tsx` — سطر 65 فقط.

